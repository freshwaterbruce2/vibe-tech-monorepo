{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/_-factory-runtime-smoke && pnpm run build:shared:api && pnpm run build:api",
    "watchPatterns": [
      "apps/_-factory-runtime-smoke/**",
      "packages/auth/**",
      "packages/analytics/**",
      "packages/billing/**",
      "packages/emails/**",
      "packages/entitlements/**",
      "packages/landing/**"
    ]
  },
  "deploy": {
    "startCommand": "cd apps/_-factory-runtime-smoke && pnpm run start:api",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE"
  }
}
