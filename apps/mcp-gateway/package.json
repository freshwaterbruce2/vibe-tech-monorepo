{
  "name": "@vibetech/mcp-gateway",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP Gateway: bridges OpenClaw (Brain) with Antigravity MCP servers (Environment) via IPC Bridge",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "catalog:",
    "@vibetech/logger": "workspace:*",
    "@vibetech/shared-ipc": "workspace:*",
    "ws": "^8.20.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@types/ws": "^8.18.1",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
