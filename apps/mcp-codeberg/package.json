{
  "name": "mcp-codeberg",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "DEPRECATED: MCP server for Codeberg API. Repo migrated to GitHub. Consider using GitHub MCP instead.",
  "scripts": {
    "build": "tsc -p tsconfig.json --incremental false",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.0",
    "axios": "^1.13.6",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "5.9.3"
  }
}
