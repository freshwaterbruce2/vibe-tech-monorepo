/**
 * Re-export Button from @vibetech/ui shared component.
 * This file exists for backward-compatible imports via "@/components/ui/button".
 * The shared component provides: default, destructive, outline, secondary, ghost, link,
 * neon, vibetech variants + isLoading, icon, asChild props.
 */
export { Button, buttonVariants, type ButtonProps } from '@vibetech/ui';
