[pytest]
testpaths = vibe_justice/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Strict markers to avoid typos
# Wave 2: --cov-fail-under raised from 30 to 60; target 80 once legacy modules have tests.
addopts =
    -v
    --strict-markers
    --tb=short
    -ra
    --cov=vibe_justice
    --cov-report=term-missing
    --cov-report=html:htmlcov
    --cov-fail-under=60

markers =
    unit: Unit tests (fast, no external dependencies)
    integration: Integration tests (may use database/filesystem)
    slow: Slow tests (may take >5 seconds)
    security: Security-related tests
    api: API endpoint tests

# Async configuration
asyncio_mode = auto

# Filter warnings
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning

# Minimum version
minversion = 7.0

# Test discovery
norecursedirs =
    .git
    .venv
    __pycache__
    dist
    build
    *.egg-info

# Coverage configuration
[coverage:run]
source = vibe_justice
omit =
    */tests/*
    */test_*.py
    */__pycache__/*
    */.venv/*
    */migrations/*

[coverage:report]
precision = 2
skip_empty = True
exclude_lines =
    pragma: no cover
    def __repr__
    raise AssertionError
    raise NotImplementedError
    if __name__ == .__main__.:
    if TYPE_CHECKING:
    @abstractmethod
