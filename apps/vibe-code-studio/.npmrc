# NOTE: For Electron packaging, better-sqlite3 must be available in node_modules
# The build scripts copy from monorepo root where pnpm installs it
# Use rebuild-deps script before packaging to compile for Electron ABI
