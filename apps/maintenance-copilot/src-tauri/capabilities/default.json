{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main-window capability. The gateway sidecar is spawned from Rust (lib.rs); the scoped shell:allow-spawn below additionally permits the frontend to (re)launch it if ever needed.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-spawn",
      "allow": [{ "name": "binaries/gateway", "sidecar": true, "args": false }]
    }
  ]
}
