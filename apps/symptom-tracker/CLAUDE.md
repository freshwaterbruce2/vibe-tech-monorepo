# symptom-tracker — AI Context

## What this is
Health symptom tracking app — stub/placeholder directory with only a node_modules symlink present; no source code has been scaffolded yet.

## Stack
- **Runtime**: Node.js 22 (inferred from node_modules: @rolldown, @vitejs)
- **Framework**: Likely Vite + React (inferred from node_modules); no source exists yet
- **Key deps**: Unknown — no package.json present

## Dev
```bash
# No runnable scripts exist yet — app has not been scaffolded
```

## Notes
- Only `node_modules/` directory exists; no `package.json`, `src/`, or any source files
- The presence of `@vitejs` in node_modules suggests a Vite-based app is planned
- Do not create duplicate files — scaffold from scratch if implementing this app
