# Monetization and Database Reference - Dental Clinic Scheduler

This document was automatically generated by the VibeTech App Factory to map the monetization plans, features, database schemas, and configuration variables for Dental Clinic Scheduler.

## Pricing Tiers

The following plans are defined for Dental Clinic Scheduler:

| Plan | Price (Monthly) | Included Features |
| ---- | --------------- | ----------------- |

| **Free** | Free | scheduling.automated_slots, notifications.sms_reminders, patient_management.dashboard |

| **Starter** | $29.99 | scheduling.automated_slots, notifications.sms_reminders, patient_management.dashboard, scheduling.online_booking |

| **Pro** | $79.99 | scheduling.automated_slots, notifications.sms_reminders, patient_management.dashboard, scheduling.online_booking, staff_management.dentist_profiles |

## Feature Flags and Entitlements

The application resolves the following feature keys to check user permissions:

| Feature Key | Display Name | Description |
| ----------- | ------------ | ----------- |

| `scheduling.automated_slots` | Automated Appointment Slot Generation | Automatically generates available appointment slots based on dentist availability and clinic operating hours. |

| `notifications.sms_reminders` | SMS Appointment Reminders | Sends automated SMS reminders to patients before their appointments. |

| `patient_management.dashboard` | Patient Management Dashboard | A centralized dashboard to view and manage patient information, appointment history, and notes. |

| `scheduling.online_booking` | Online Patient Booking Portal | Allows patients to book appointments online through a dedicated portal. |

| `staff_management.dentist_profiles` | Dentist Profile Management | Manage profiles and availability for multiple dentists within the clinic. |

## Database Schema

On boot, the application initializes the following SQLite tables:

### Table: `users`

| Column | Definition |
| ------ | ---------- |

| `id` | `UUID` |

| `email` | `VARCHAR(255)` |

| `password_hash` | `VARCHAR(255)` |

| `role` | `VARCHAR(50)` |

| `created_at` | `TIMESTAMP` |

| `updated_at` | `TIMESTAMP` |

### Table: `patients`

| Column | Definition |
| ------ | ---------- |

| `id` | `UUID` |

| `first_name` | `VARCHAR(255)` |

| `last_name` | `VARCHAR(255)` |

| `phone_number` | `VARCHAR(20)` |

| `email` | `VARCHAR(255)` |

| `date_of_birth` | `DATE` |

| `address` | `TEXT` |

| `notes` | `TEXT` |

| `created_at` | `TIMESTAMP` |

| `updated_at` | `TIMESTAMP` |

### Table: `dentists`

| Column | Definition |
| ------ | ---------- |

| `id` | `UUID` |

| `user_id` | `UUID` |

| `first_name` | `VARCHAR(255)` |

| `last_name` | `VARCHAR(255)` |

| `specialty` | `VARCHAR(255)` |

| `phone_number` | `VARCHAR(20)` |

| `email` | `VARCHAR(255)` |

| `created_at` | `TIMESTAMP` |

| `updated_at` | `TIMESTAMP` |

### Table: `appointments`

| Column | Definition |
| ------ | ---------- |

| `id` | `UUID` |

| `patient_id` | `UUID` |

| `dentist_id` | `UUID` |

| `start_time` | `TIMESTAMP` |

| `end_time` | `TIMESTAMP` |

| `status` | `VARCHAR(50)` |

| `notes` | `TEXT` |

| `created_at` | `TIMESTAMP` |

| `updated_at` | `TIMESTAMP` |

### Table: `sms_notifications`

| Column | Definition |
| ------ | ---------- |

| `id` | `UUID` |

| `appointment_id` | `UUID` |

| `patient_id` | `UUID` |

| `message_body` | `TEXT` |

| `sent_at` | `TIMESTAMP` |

| `status` | `VARCHAR(50)` |

## Environment Configuration

To run Dental Clinic Scheduler with full payment processing and billing checks, configure the following environment variables in your local `.env` or deployment runtime:

```bash
# The port and host for Fastify backend server
PORT=5320
HOST=127.0.0.1

# Stripe integration keys (required for Stripe setup and subscriptions)
STRIPE_SECRET_KEY=your_stripe_test_secret_key
STRIPE_PUBLISHABLE_KEY=your_stripe_test_publishable_key
STRIPE_WEBHOOK_SECRET=your_stripe_local_webhook_secret

# OpenRouter credentials (for AI assistant features)
OPENROUTER_API_KEY=your_openrouter_api_key

# Cookie session credentials
AUTH_SECRET=a_secure_cookie_session_secret_at_least_32_chars
```

## First Launch Sequence

When Dental Clinic Scheduler starts up for the first time, it performs a touchless provisioning check:

1. **Stripe Price Synchronization**: The app connects to the Stripe API and creates products and monthly pricing records matching the plans listed above.
2. **Operator Registration**: The system provisions a Stripe Customer matching `DEMO_USER_EMAIL` (defaulting to `owner@example.com`).
3. **Starter Subscription**: An initial subscription is automatically created for the operator customer subscribing them to the first paid tier (e.g. `pro`).
4. **Database Table Creation**: The server configures its local SQLite WAL database and runs the table generation migrations.
