# clawdbot-desktop — AI Context

## What this is
Desktop application shell for the Clawdbot AI assistant — a stub/placeholder directory with only node_modules present; no source code has been scaffolded yet.

## Stack
- **Runtime**: Node.js 22 (inferred from node_modules presence)
- **Framework**: Unknown — no source files exist yet
- **Key deps**: Unknown (node_modules directory exists but no package.json)

## Dev
```bash
# No runnable scripts exist yet — app has not been scaffolded
```

## Notes
- Only `node_modules/` exists; no `package.json`, `src/`, or entry point
- Do not create duplicate files — scaffold from scratch if implementing
- Likely intended as an Electron or Tauri desktop wrapper for a chat/agent UI
