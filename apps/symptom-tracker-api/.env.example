# REQUIRED: must be on D:\ per repo storage rules
DATABASE_PATH=D:\data\symptom-tracker\symptom-tracker.db

# Optional
PORT=5055
HOST=127.0.0.1

# Remote use tip (do NOT open router ports):
# - Use Tailscale and set HOST=0.0.0.0

# Optional: serve built UI from the API (defaults to apps/symptom-tracker/dist)
# UI_DIST_PATH=C:\dev\apps\symptom-tracker\dist
