import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vibetech.healthtracker',
  appName: 'Health Tracker',
  webDir: 'dist'
};

export default config;
