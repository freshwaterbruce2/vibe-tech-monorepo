{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@vibetech/hooks",
  "projectType": "library",
  "root": "packages/vibetech-hooks",
  "sourceRoot": "packages/vibetech-hooks/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "cache": true,
      "options": {
        "command": "tsc",
        "cwd": "packages/vibetech-hooks"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec eslint src --ext .ts,.tsx --max-warnings 0",
        "cwd": "packages/vibetech-hooks"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "packages/vibetech-hooks"
      }
    },
    "quality": {}
  },
  "tags": [
    "type:library",
    "scope:vibetech"
  ]
}
