{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@nova/types",
  "projectType": "library",
  "root": "packages/nova-types",
  "sourceRoot": "packages/nova-types/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "cache": true,
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "packages/nova-types"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": {
        "command": "vitest run",
        "cwd": "packages/nova-types"
      }
    },
    "test:coverage": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --coverage",
        "cwd": "packages/nova-types"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec eslint src --ext .ts,.tsx --max-warnings 0",
        "cwd": "packages/nova-types"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "packages/nova-types"
      }
    },
    "quality": {}
  },
  "tags": [
    "type:library",
    "scope:nova"
  ]
}
