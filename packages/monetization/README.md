# monetization

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build monetization` to build the library.

## Running unit tests

Run `nx test monetization` to execute the unit tests via [Vitest](https://vitest.dev/).
