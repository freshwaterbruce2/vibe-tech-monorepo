import { avatarRenderEngine } from './avatar-render-engine';

describe('avatarRenderEngine', () => {
  it('should work', () => {
    expect(avatarRenderEngine()).toEqual('avatar-render-engine');
  });
});
