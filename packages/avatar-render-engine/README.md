# avatar-render-engine

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build avatar-render-engine` to build the library.

## Running unit tests

Run `nx test avatar-render-engine` to execute the unit tests via [Vitest](https://vitest.dev/).
