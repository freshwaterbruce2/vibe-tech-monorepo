{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@nova/database",
  "projectType": "library",
  "root": "packages/nova-database",
  "sourceRoot": "packages/nova-database/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "cache": true,
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "packages/nova-database"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": {
        "command": "vitest run",
        "cwd": "packages/nova-database"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec eslint src --ext .ts,.tsx --max-warnings 0",
        "cwd": "packages/nova-database"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "packages/nova-database"
      }
    },
    "quality": {}
  },
  "tags": [
    "type:library",
    "scope:nova"
  ]
}
