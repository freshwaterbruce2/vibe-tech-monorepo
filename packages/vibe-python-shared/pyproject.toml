[tool.poetry]
name = "vibe-python-shared"
version = "0.1.0"
description = "Shared Python schemas for the Vibe-Tech IPC Bridge"
authors = ["Vibe-Tech Architect"]

[tool.poetry.dependencies]
python = "^3.10"
pydantic = "^2.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
